<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.Diary_app_draft">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="+@string/Diary_App"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplicationDsdsd">
        <activity
            android:name=".Login_reg.Signin"
            android:exported="true" />
        <activity
            android:name=".NewentryActivity"
            android:exported="true" />
        <activity
            android:name=".FolderActivity"
            android:exported="false" />
        <activity
            android:name=".Login_reg.SignupActivity"
            android:exported="true" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false"/>

        <activity
            android:name=".Login_reg.WelcomePageSlideActivity"
            android:exported="true" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>